<h1>New Franchise</h1>

<%= form_with model: @franchise do |f| %>
  <p>
    <%= f.label(:name, "Name") %> <br />
    <%= f.text_field :name %>
  </p>
  <p>
    <%= f.submit "Create" %>
  </p>
<% end %>

<p>
  <span class="symbol">❌</span> <%= link_to 'Cancel', franchises_path %>
</p>
