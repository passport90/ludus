<div class="breadcrumb">
  <div class="breadcrumb__el">
    <%= link_to 'Games', years_path%>
  </div>
  <div class="breadcrumb__sep">
    >
  </div>
  <div class="breadcrumb__el">
    <%= @year %>
  </div>
</div>

<h1><%= @year %></h1>

<% if @weeks.length > 0 %>
  <ol class="week-list">
    <% @weeks.each do |week| %>
      <li class="week-list__el">
        <%= 
          link_to(
            "Week #{week[:week_num]}, #{week[:week_start].strftime('%-d %B')}"\
            " - #{(week[:week_start] + 6.days).strftime('%-d %B')}",
            games_path(year: @year, week: week[:week_num])
          )
        %>
      </li>
    <% end %>
  </div>
<% else %>
  <p>There are no games this year.</p>
<% end %>


<%= link_to '+', new_game_path, class: 'create-button' %>