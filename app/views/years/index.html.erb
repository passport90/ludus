<h1>Games</h1>

<p>
  <span class="symbol">🏠</span> <%= link_to 'Home', root_path %> <br />
  <span class="symbol">➕</span> <%= link_to 'Create New', new_game_path %>
</p>
<p>
  <% if @years.length > 0 %>
    Years: <br />
    <% @years.each do |year| %>
      <%= link_to year, weeks_path(year: year) %><br />
    <% end %>
  <% else %>
    There are no games.
  <% end %>
</p>